version: '3.8'
services:
  db:
    container_name: clj_game_geek
    image: postgres:12.3-alpine
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: clj_game_geek_db
